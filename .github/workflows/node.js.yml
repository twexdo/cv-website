
on:
  push:
    branches:
      - master

jobs:
  deploy:
    runs-on: self-hosted
   
    steps:
    - name: NVM Setup Node
      run: pwd
      
    - name: Set up node using nvm
      uses: irby/setup-node-nvm@v1.0.0
         node-version:16

 

  
